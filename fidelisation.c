#include<stdio.h>
#include<stdlib.h>


int main()
{

    char reponse;
    // pour effectuer la tache fidelisation en affiche la liste des clients par ordre descandant, en ajoute 1,3 % au montant des 3premiers compte.
    fonction ( liste descandant);

   printf("fidelisation, saisissez Y ou N ?\n");
   printf("Y-yes");
   printf("N-no");
   scanf("%c", &reponse );

   if (reponse = Y){
      Montant = Montant + ((Montant * 1,3)/100);   
   }

   else (){ 
       goto fidelisation;
   }
    
   